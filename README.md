# EFFICIENT-DETECTION-OF-EYE-DISEASES-USING-AI-AND-ML
Our system includes three stages: pre-processing, feature extraction using convolutional neural networks (CNNs), and classification with ML and DL algorithms (SVM, RF, DNN). Rigorous testing on a dataset with various eye diseases shows our method outperforms current approaches in accuracy, sensitivity, and specificity..
